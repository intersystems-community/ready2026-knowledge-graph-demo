# PlanetCare Knowledge Base

Internal KB articles for PlanetCare EMR support advisors.

Articles in this directory are generated automatically from clustered support tickets.
Each article traces back to the tickets that produced it — see the knowledge graph for provenance.

| Category | Articles |
|----------|---------|
| BILLING | (empty — run notebook to generate) |
| LABORATORY | (empty) |
| PHARMACY | (empty) |
| PRINTING | (empty) |
| INTERFACING | (empty) |

Run `notebooks/planetcare_clustering_demo.ipynb` to populate this wiki.
